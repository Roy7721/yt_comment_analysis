# ── Core data / ML ──────────────────────────
numpy<2.3
pandas==2.3.3
scikit-learn==1.9.0
scipy==1.17.1
joblib==1.5.3

# ── NLP feature extraction ──────────────────
spacy==3.8.14
nltk==3.10.0
# spaCy English model, pinned as a wheel → installs via pip, no `spacy download` step needed
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# ── kept ONLY because build_features.py & model_building.py do `import lightgbm` ──
lightgbm==4.7.0

# ── Plotting (evaluation.py confusion matrix, app.py) ──
matplotlib==3.11.1
seaborn==0.13.2

# ── Tracking / registry / config ────────────
mlflow==3.14.0
python-dotenv==1.2.2
PyYAML==6.0.3

# ── Pipeline orchestration ──────────────────
dvc

# ── Serving (Flask API) ─────────────────────
Flask==3.1.3
flask-cors==6.0.5
waitress==3.0.2